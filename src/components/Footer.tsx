import { Box, Text } from 'ink';
import type Input from './Input.js';

const Footer = () => (
  <Box flexDirection="column" gap={1}>
    <Text color="cyan">Footer</Text>
  </Box>
);

export default Footer;
